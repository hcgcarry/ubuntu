""""something need to check
所有的 SpaceVim 选项可以使用 :h SpaceVim-config 来查看。选项名称未原先 Vim 脚本中使用的变量名称去处 g:spacevim_ 前缀。
Bookmarks
"""

install ctag to enable F2
"""""""'add layer

""""""""""""""command

""""""""note
spc + s 出現的視窗可以用ctrl + i切換過去"
